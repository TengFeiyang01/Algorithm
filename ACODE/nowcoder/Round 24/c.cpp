#include <bits/stdc++.h>

const int inf = 0x3f3f3f3f;
using i64 = long long;

void solve() {
    /*
	def solve() -> None:
	    s = input()
	    i = 0
	    n = len(s)
	    ans = 0.0
	    while i < n:
	        j = i
	        while j < n and s[j] != '.':
	            j += 1
	        if j == n:
	            ans += float(s[i:j])
	            break
	        j += 2
	        ans += float(s[i:j])
	        i = j
	    print(ans)
    */
}

int main() {
    std::cin.sync_with_stdio(false);
    std::cin.tie(0);
    
    solve();

    return 0;
}
