#include <bits/stdc++.h>
 
const int inf = 0x3f3f3f3f;
using i64 = long long;

void solve() {
    int a, b, c;
    std::cin >> a >> b >> c;
    std::cout << 2 * a + 3 * b + 5 * c << "\n";
}

int main() {
    std::cin.sync_with_stdio(false);
    std::cin.tie(0);

    solve();

    return 0;
}