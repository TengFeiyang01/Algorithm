#include <bits/stdc++.h>

const int inf = 0x3f3f3f3f;
using i64 = long long;

void solve() {
    i64 k;
    std::cin >> k;
    int sx, sy, tx, ty;
    std::cin >> sx >> sy >> tx >> ty;
}

int main() {
    std::cin.sync_with_stdio(false);
    std::cin.tie(0);
    
    solve();

    return 0;
}
