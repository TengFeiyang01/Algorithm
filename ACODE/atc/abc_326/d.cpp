#include <bits/stdc++.h>
 
const int inf = 0x3f3f3f3f;
using i64 = long long;

void solve() {
    int N, R, C;
    std::cin >> N >> R >> C;
    std::vector<std::string> s(N, std::string(N, '.'));

}

int main() {
    std::cin.sync_with_stdio(false);
    std::cin.tie(0);

    solve();

    return 0;
}