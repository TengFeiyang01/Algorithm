#include <bits/stdc++.h>

int main() {
    //static_cast 在一个方向上可以隐式转换，另一个方向上，可以显示的静态转换

    /*
    double d = 5.5;
    int i = 4;
    //双隐
    d = static_cast<double>(i);
    i = static_cast<int>(d);

    d = static_cast<double>(10) / 3;
    std::cout << d << " " << i << "\n";

    //单隐
    void *p;
    int *q;
    q = (int*)malloc(sizeof(10) * 10);
    q = static_cast<int*>(malloc(10 * 19));
    p = q;
//    q = p; //失败
    q = static_cast<int*>(p);

    //双不隐
    int *m;
    int n;
//    m = n;
//    n = m;
//    m = static_cast<int*> (n);
//    n = static_cast<int> (m);
    */

    return 0;
}
